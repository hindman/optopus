--------
dev-notes: Dev notes
--------

Creating a new virtualenv:

    v='2.7.18'
    deactivate
    pyenv shell $v && pyenv versions
    mkvirtualenv optopus-$v
    full-venv
    pip install -e . -e .[dev] -e .[test]

Activate and run tests:

    # Basic.

    workon optopus-3.8.5      # Or 2.7.18
    pytest -s -v tests/

    # With tox.

    pyenv shell 3.8.5  2.7.18
    tox

